# User config (may contain a personal Last.fm API key) — never commit
config.json

# Local artwork library + its manifest + songlist snapshot
library/
cache/

# Logs (rotation adds artwork_fetcher.log.1 … .3)
*.log
*.log.*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.idea/
.vscode/

# Local design notes (kept out of the public repo)
PLAN.md
MIGRATION.md
