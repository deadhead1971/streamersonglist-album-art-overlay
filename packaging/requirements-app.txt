# Exact pins for everything the installed app ships (packaging/build.ps1
# installs these into the bundled Python's site-packages). The app's own ranges
# are in requirements.txt; bump these deliberately and rebuild, so a release
# never picks up a new dependency version nobody has run.
# Refresh: pip install requirements.txt + pystray into a clean venv, run the
# pre-release checks, then pip freeze into this file.
blinker==1.9.0
centrifuge-python==0.6.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
Flask==3.1.3
idna==3.20
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
pillow==12.3.0
protobuf==7.36.2
pystray==0.19.5
requests==2.34.2
six==1.17.0
urllib3==2.8.0
websockets==16.1.1
Werkzeug==3.1.8
